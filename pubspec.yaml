name: drawly
description: "A new Flutter project."

publish_to: "none"

version: 0.9.4

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  draw_board:
    path: packages/draw_board
  drawly_design_system:
    path: packages/drawly_design_system
  drawly_core:
    path: packages/drawly_core
  universal_platform: ^1.1.0
  flutter_inset_box_shadow: ^1.0.8
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/svgs/
